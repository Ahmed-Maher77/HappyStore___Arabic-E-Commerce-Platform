import Loader from "./Loader";

const MainLoader = () => {
	return (
		<div className="Main-Loader h-screen w-full flex items-center justify-center">
			<Loader />
		</div>
	);
};

export default MainLoader;
